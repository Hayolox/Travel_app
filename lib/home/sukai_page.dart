import 'package:flutter/material.dart';

class SukaiPage extends StatelessWidget {
  const SukaiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Sukai Page'));
  }
}
